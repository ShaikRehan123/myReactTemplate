import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    brand: {
      primary: "#e2f3f5",
      secondary: "#22d1ee",
      tertiary: "#0e153a",
      quaternary: "#3d5af1",
    },
  },
});

export default theme;
